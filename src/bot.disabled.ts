/*
  Archivo `bot.ts` deshabilitado: el proyecto ya no usa Telegram.
  Se conserva aquí como referencia. Si en el futuro decides reactivar
  el bot de Telegram, renombra este archivo a `bot.ts` y reinstala
  la dependencia `telegraf`.

  (Contenido original movido aquí para histórico.)
*/

// Contenido original movido a este archivo para referencia.
